---
const { class: className, ...props } = Astro.props;
---

<select
    class={`shrink-0 w-auto min-w-[100px] md:min-w-[200px] max-w-[250x] h-9 rounded-[28px] px-4 border-none text-sm bg-[#ecfbfd] text-[#333]
        hover:bg-[#299037] hover:text-white hover:shadow-md
        focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00acc1]
        ${className || ""}`}
    {...props}
>
    <slot />
</select>
